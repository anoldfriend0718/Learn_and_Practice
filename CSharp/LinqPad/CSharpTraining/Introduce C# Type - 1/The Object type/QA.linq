<Query Kind="Statements" />

"QA".H1();

"If equals still valid, but change GetHashCode always return 1, does it still work fine?".Info();
"Why we need check type equality inside the equals method?".Info();